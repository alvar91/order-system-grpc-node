# Используем образ Node.js версии 18 в качестве базового.
FROM node:18 

# Устанавливаем рабочую директорию внутри контейнера.
WORKDIR /usr/src/app 

# Копируем файлы package.json и package-lock.json в рабочую директорию.
COPY package*.json ./ 

# Устанавливаем зависимости проекта, указанные в package.json.
RUN npm install 

# Копируем все файлы из текущей директории на хосте в рабочую директорию контейнера.
COPY . . 

# Открываем порт 5002 для внешнего взаимодействия с контейнером.
EXPOSE 5002

# Указываем команду для запуска приложения при старте контейнера.
CMD npm start 
